{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://design.local/schemas/tokens.schema.json",
  "title": "Normalized Design Roles",
  "type": "object",
  "required": ["schema_version", "tokens"],
  "properties": {
    "schema_version": {"const": "1.0"},
    "tokens": {
      "type": "array",
      "minItems": 4,
      "items": {
        "type": "object",
        "required": ["name", "category", "role", "value", "exact", "evidence_class", "source_evidence_ids", "notes"],
        "properties": {
          "name": {"type": "string", "minLength": 3},
          "category": {"enum": ["color", "typography", "spacing", "radius", "border", "shadow", "motion", "layout", "component", "media"]},
          "role": {"type": "string", "minLength": 20},
          "value": {},
          "exact": {"type": "boolean"},
          "evidence_class": {"enum": ["observed", "measured", "inferred", "estimated", "recommended", "unknown"]},
          "source_evidence_ids": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string"}},
          "notes": {"type": "string", "minLength": 3}
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
